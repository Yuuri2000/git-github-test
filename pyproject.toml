[project]
name = "git-github-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yuuri", email = "shankouliyinai@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
git-github-test = "git_github_test:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
